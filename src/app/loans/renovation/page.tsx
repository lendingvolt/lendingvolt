import { StubPage, stubMetadata } from "@/components/site/stub-page";

export const metadata = stubMetadata("renovation");

export default function Page() {
  return <StubPage page="renovation" />;
}
