import { StubPage, stubMetadata } from "@/components/site/stub-page";

export const metadata = stubMetadata("applications");

export default function Page() {
  return <StubPage page="applications" />;
}
